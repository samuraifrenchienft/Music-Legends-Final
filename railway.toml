[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
startCommand = "python run_bot.py"
# Force rebuild - change this to trigger new deployment

[[services]]
name = "music-legends-bot"

[services.variables]
PYTHON_VERSION = "3.11"
